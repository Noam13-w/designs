v2: Fix dark mode: ensure Tailwind config uses proper dark mode strategy (class), update JS to toggle 'dark' class on <html> element and persist via localStorage, fix theme toggle button to properly swap SVG icons without breaking container. Make sure all colors, borders, and backgrounds respect dark mode variants while keeping #0EA5E9 as the accent color. Also fix sidebar text overflow at collapsed width by hiding labels and showing icons only. Ensure map legend uses brand-justified colors, not default Tailwind shades.