v2: Update hero headline to exactly: Stop chasing your drivers. Start scaling your fleet. Add a dark/light mode toggle in the header (persists via localStorage). Switch font family to Inter (fallback: system-ui, sans-serif). Replace all primary accent uses of the current blue with #0EA5E9 (sky blue) across buttons, links, hover states, and accents. Ensure dark mode uses appropriate contrasting shades while keeping #0EA5E9 as the accent color.